<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mtms.mapper.MyReviewMapper">
	<select id="getTotalCount" resultType="int">
	  <![CDATA[
     	SELECT COUNT(*) FROM review WHERE    
     	reviewNo > 0 
     ]]>
   </select>

<select id="getListWithPaging" resultType="com.mtms.domain.ReviewVO">
		<![CDATA[
		SELECT
			movieNo,reviewTitle,memberId,reviewView,reviewDate, reviewNo
			from 
				(
				select /*+INDEX_DESC(review PK_review )*/
				rownum rn,movieNo,reviewTitle,memberId,reviewView,reviewDate,reviewNo
				from 
				review
				where
	
            rownum <= #{cri.pageNum} * #{cri.amount}
            ) WHERE rn > (#{cri.pageNum} - 1) * #{cri.amount} AND memberId = #{memberId}
      ]]> 
   </select>
</mapper>