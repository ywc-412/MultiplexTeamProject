<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mtms.mapper.ScheduleMapper">

	<insert id="insert">
	
	</insert>
	
	<update id="update">
	
	</update>
	
	<delete id="delete">
	
	</delete>
	
	<select id="get" resultType="com.mtms.domain.ScheduleVO">
		SELECT * FROM schedule WHERE to_char(scheduleDate, 'yyyymmdd') = #{scheduleDate} and screen = #{screen}
<!-- 		SELECT * FROM schedule WHERE to_char(scheduleDate, 'yyyymmdd') = '20200115' -->
<!-- 		#{scheduleDate} --> 
	</select>
</mapper>